alt + s // add new node 
shift + h //check affected color in fusion 
control + y // to add version 
control + b // to change versions one by one.
select new vedio in color tab then click on any other vedio that have colors done then press scorl mouse center button. the colors will apply to this. 
